---
# common vars
git_mirrors: [] # array of {src: <src_repo_slug>, [dest: <dest_repo>]}
git_mirror_dir: "git-repos"
git_mirror_action: "mirror"

# download.yml vars

# upload.yml vars
#git_mirror_default_dest: null # must be specified
