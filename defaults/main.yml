---
# common vars
git_mirror_dir: "git-repos"
git_mirror_action: "download"

# download.yml vars
git_mirrors: []

# upload.yml vars
git_mirror_dest: ''
git_mirror_custom_dest: [] # array of {src: <src_repo_slug>, dest: <dest_repo>}
