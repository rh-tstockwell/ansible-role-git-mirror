---
- hosts: localhost
  roles:
    - git-mirror
  vars:
    git_mirror_action: upload
    git_mirrors:
      - https://github.com/stocky37/ansible-role-git-mirror.git
    git_mirror_dir: /tmp/git-repos
    git_mirror_dest: https://github.com/stocky37-test
