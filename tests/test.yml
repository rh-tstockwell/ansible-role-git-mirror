---
- hosts: localhost
  roles:
    - git-mirror
  vars:
    git_mirror_action: upload
    git_mirrors:
      - src: https://github.com/stocky37/ansible-role-git-mirror.git
        dest: custom
    git_mirror_dir: /tmp/git-repos
    git_mirror_default_dest: https://github.com/stocky37
